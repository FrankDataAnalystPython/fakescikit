After the setup.py and structure is done, then we could try the following to install locally

pip install .

If successfully, we couild start publishing on PyPi

If we wanted to push things to github, then do

git add .
git commit -am 'your comment of this push'


# Study git, how to clone someones git into local directory
    git clone url
#
